from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Deep Learning application to locate a toy at ha household',
    author='UC Berkeley',
    license='BSD-3',
)
